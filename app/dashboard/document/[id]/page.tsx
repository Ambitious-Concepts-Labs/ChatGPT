import 'server-only'
import { NewDocument} from "../(components)/components";

function DocumentPage() {
  return (
    <div  className="flex h-full w-full">
      <NewDocument/>
    </div>
  );
}

export default DocumentPage;